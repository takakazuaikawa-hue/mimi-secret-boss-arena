# エージェント作業規約

このリポジトリは Codex と Claude Code を併用して制作している。
このファイルが唯一の規約実体で、`CLAUDE.md` はここへの参照。
どちらのツールで作業する場合も、まずこの内容に従う。

## このプロジェクト

「ミミのときめき裏ボス闘技場」。週刊育成シミュレーションと監督型3対3
オートバトルを組み合わせたブラウザゲーム。1周26週、初回75〜90分想定。

- 全体像: [README.md](README.md)
- 遊びの設計: [docs/GAME_DESIGN.md](docs/GAME_DESIGN.md)
- 技術と所有境界: [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)
- 文章の基準と人物の声: [docs/TEXT_BIBLE.md](docs/TEXT_BIBLE.md)
- 監査の採点と計測値: [docs/QUALITY_AUDIT.md](docs/QUALITY_AUDIT.md)

## 進行の作法

[docs/WORKING_PROTOCOL.md](docs/WORKING_PROTOCOL.md) が制作進行の規約。要点:

- 返答ごとに「作業継続中 / 確認待ち / 実装・検証完了 / 阻害中」のどれかを明示する。
- 「次は進めます」と予告して返答を終えない。確認が必要な判断以外では止まらない。
- 設計書を作っただけで、実装まで完了したように報告しない。
- ビルドが通っただけで実画面確認まで済んだとみなさない。

## 併用ルール

- **交代制で作業する。** Codex と Claude Code を同時に同じ作業ツリーで走らせない。
  片方が作業中はもう片方を止める。
- 作業を交代するときは、変更済みファイルと未完了項目をこのファイルの
  「## 引き継ぎ」節に書き残す。
- このリポジトリは Codex サンドボックスのユーザー所有になっている。git が
  `dubious ownership` で失敗する場合は
  `git config --global --add safe.directory C:/Users/takakazu/projects/mimi_secret_boss_arena`
  を実行する。

## コマンド

```powershell
npm run dev        # 開発サーバー
npm test           # vitest 全件（周回・戦闘監査を含む。maxWorkers=1）
npm run typecheck  # tsc --noEmit
npm run build      # 型検査 + 本番ビルド
npm run qa:onboarding   # Playwright による導入部QA
npm run qa:visual       # Playwright による画面QA
```

対象を絞る場合は `npm run audit:content` / `npm run audit:sim`。

## 公開（GitHub Pages）

公開URL: https://takakazuaikawa-hue.github.io/mimi-secret-boss-arena/

ゲームはアセットを `/assets/...` の絶対パスで参照するため、サブパス配信では
`npm run build:pages` を使う（`--base=./` + `scripts/patch-relative-asset-paths.mjs`
で配信物のみ相対化。ソースの絶対パス規約は変更しない）。

再デプロイ手順:

```powershell
npm run build:pages
```

その後、dist の内容を `gh-pages` ブランチへコミットして push する
（LFSフィルタを避けるため、.gitattributes を含めない別ディレクトリで行う）。
push すると Pages が自動で再ビルドする。

## 所有境界（変更時の原則）

[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) の所有境界に従う。特に:

- `src/data/` と `src/narrative/`: 文章・コンテンツ。**進行ロジックを変更しない。**
- `src/game/engine.ts` `battle.ts`: ロジック。**保存形式を変更しない。**
- `src/game/types.ts`: コンテンツと保存データの契約。変更は影響範囲を確認してから。
- `src/game/content.ts`: Zod による起動時監査。コンテンツ追加時は総数の更新も要る。

`balanceAudit.test.ts` の閾値は、意図した設計変更のとき以外は緩めない。

## 完了の条件

- 実装: コード変更 + `npm run typecheck` または対象テストを通した。
- 画面: デスクトップと 390px 幅の実画面を確認した。コンソールエラーなし。
- 物語: 前後の文脈、選択の結果、必要な演出資産まで記述した。
- 未確認の項目がある場合は、その項目だけを未完了として明示的に残す。

## 引き継ぎ

（作業を中断・交代するときにここへ追記する）

### 2026-08-09 Claude Code → 次作業者へ

**物語改稿プロジェクト完走**: 全15人のv2改稿が完了した（コミット `bc4260a`）。
基準は docs/NARRATIVE_REBOOT_PLAN.md（高橋留美子テイスト×現代的、高校生が読める
ライトノベル文体）、世界の織り方は docs/WORLD_CONTINUITY.md が正本。

- 最終波（マリアン・十七号室・リンネ・無銘）28場面を改稿。演目表は全15幕 final
  （`src/data/openingProgram.ts`）。封筒・演目・モチーフ台帳も登記済み。
- 承認済み後日談3本（治さない相談日・元手ゼロ杯・所有者なし搬出式）は骨格保持で
  出演届の一行のみ追加。改稿ブロックの debug.status はすべて "review"。
- 検証: 対象テスト36件（narrative/content/program/saveMigration）と typecheck 合格。
  全スイート（周回・戦闘監査）は実行済みだが完走確認は次作業者に委ねる場合あり
  （物語JSONのみの変更なので戦闘監査への影響は想定なし）。
- GitHub Pages 再デプロイ済み（gh-pages `cb585aa`、改稿後の物語が公開版に反映）。

### 2026-08-10 Claude Code → 次作業者へ(三段階キャンペーン)

- 三段階キャンペーン実装済み(`src/data/campaignStages.ts` 正本、周回持ち越し・
  区分選び直し・主軸優先)。三周の感情設計は docs/GAME_DESIGN.md §4 が正本。
- メインストーリー全15話(各区分5話、`src/narrative/content/main.s*.ep*.json`)を
  二段再生(メイン→個別meet)で接続済み。対応表は engine.ts の stageOneMainEpisodes。
- 執筆原則(ユーザー指導による): 面白さ基準/謎の告知禁止(種は一粒・ギャグの
  テンポ)/全ノード「ミミが行動→世界が反応」の因果場面。observancは
  メモリ fun-first-fixes に詳細。
- 未実施: 区分2・3の実画面通し(機構は単体テスト済み)、第4週大会の
  「復帰戦」文脈づけ、選択フラグの後続コールバック(骨子のみ)。
- Pages再デプロイ済み(gh-pages `8904db8`)。

### 2026-08-11 Claude Code → Codex へ(現状引き渡し)

作業ツリーはクリーン、一時ファイルなし、バックグラウンドの実行タスクもゼロ。
main は `bb616c5` まで push 済み。すぐ着手できる状態。

直近で入れた修正(いずれもプレイヤー報告からの実バグ):

1. **出会いが二度読まれる**(全15話): メイン話の締めが、続けて再生される
   個別meetの冒頭と同じ場面を先に描いていた。締めを「向かう手前」で切って解消。
   再発防止に `src/game/campaignFlow.test.ts` の重複語検出テストを追加。
2. **周回で新キャラが出ない**: 区分の解放が26週の完走だけに依存していたため、
   途中で始め直すと同じ5人になった。主軸と出会った実績(周回を跨ぐ seenEvents)
   でも解放されるよう `unlockedCampaignStage()` を新設。
3. **メイン話が古い保存データで出ない**: `campaignStage` を持たない周回を
   第一勤務週として扱うよう修正。発火自体は `src/game/mainStoryFiring.test.ts` で検証。
4. **黒い封筒の反復**: 13話が同一書式で始まり予告編化していた。封筒を冒頭の
   告知から「事後に届く落ち」へ移し、圧力の到来形式を13通りに差別化。
5. **強制指示の技が読めない**: 暗色パネル化の取り残しで、白背景に白文字だった
   (`.prediction-grid` にはある指定が `.force-list` に無かった)。暗色仕様へ統一し、
   併せて技の種別・対象・説明文・MP・威力と、種別ごとの色帯を表示するようにした。
   実試合の画面で確認済み。

**検証の作法(このマシン固有)**: 戦闘監査(`balanceAudit`)と周回シミュレーションは
極端に遅く、完走に1時間以上かかる。物語・UIの変更なら
`campaignFlow` `campaignStage` `mainStoryFiring` `narrative` `content` `store` の
6ファイル(計58件)で十分。実画面の確認は `scripts/battle-ui-qa.mjs`。
入場ゲートでは必ず AUTO を選ぶこと(手動送りだと選手紹介が進まず、
指示パネルの表示条件 `!narrating` を満たせない)。

**未完了・次の候補**:
1. 区分2・3の実画面通し(機構は単体テスト済み、実画面は未確認)。
2. 真エンディング「柿落とし、十五幕」の実画面確認（3周分の通しプレイが必要。
   grand昇格ロジックは program.test.ts で検証済みだが実画面は未確認）。
3. プロローグ35ページのLN文体化（NARRATIVE_REBOOT_PLAN §6 実施計画の残項目）。
4. 選択の効果を後続場面の台詞へ反映するエンジン対応（第2波からの積み残し）。
5. debug.status "review" のブロックの人手レビュー後、"approved" への昇格。
6. 第4週の初心者大会に、メインストーリーの文脈づけを与える。
### 2026-08-16 Claude Code → 次作業者へ(3周目メイン全26話)

- **3周目メインストーリー全26話を執筆・配線完了**(main.s3.w01〜w26、旧ep1〜5は
  撤去)。台本正本は docs/MAIN_STORY_SCRIPT.md 第四部、題材研究は
  docs/MAIN_STORY_RESEARCH_S3.md。検証: typecheck+高速テスト6ファイル72件合格。
- 執筆規約の追加(ユーザー指摘、MAIN_STORY_WRITING_RULES.md §4-1b/4-1c に登記):
  お役所語は鉤括弧内のみ・ミミが直後に通訳/制作語は本文禁止/指示語は二行以上
  離れた対象を指さない/巻き戻りは日常・悲劇にしない/書類週の連打禁止/
  呼び方の台帳(監督=公の顔のみ、仲間=ミミさん、攻略対象は監督呼び禁止)。
- 実装済み(2026-08-16 追加分): grandエンディング拡張(大取りの札の場面+最終選択
  「最後の一筆」三択+女神の葉書。選択は profile.seenEvents の grand.pen.* に記録)/
  決勝の敵編成の区分差し替え(区分2=歴代王者選抜、区分3=初演の一座。matchForWeek が
  campaignStage を受ける)/本命前夜シーン10本(main.s2.eve.*・main.s3.eve.*)と
  本命システムの全区分化(maybeCreateHonmeiFollowup)。
- 未実装: 2周目の勝敗エンディング演出(新作の夜明け/再演の夜)/封(週20〜22)の
  画面演出/2・3周目とgrandエンディングの実画面通し/balanceAudit 全スイート
  (新規敵6体は機械監査未通過)/debug.status "review" の人手レビュー。
- 全話 debug.status="review"。authorNote に Q/turn/hook/carry/open のメタ行あり。

## プレイヤー向けUI・物語マニフェスト（2026-08-13）

この追加基準は `docs/PRODUCT_DIRECTION.md` と合わせて適用する。ユーザーが後から明示した
意向はさらに上位であり、実装・設計文書・過去の監査結果と衝突する場合はユーザー意向を
反映して製品基準と回帰検査を更新する。


## プレイヤーへ見せる情報

- 一画面の主役は一つにする。人物、場所、物語上の出来事、または選ぶ操作のいずれかを
  主役とし、制作側の整理語を主役にしない。
- `eyebrow`、`kicker`、英語の眉見出し、`CURRENT`、`NEXT`、`RESULT`、`STEP`、
  `STATUS`、`MEMORY` などを装飾目的で追加しない。日本語見出しとの二重表記も行わない。
- 可視ラベルを追加する前に「これがなければ初見プレイヤーが判断不能または誤操作するか」
  を確認する。答えが否なら表示しない。アクセシビリティ用の `aria-label` や tooltip は、
  必要に応じて非可視のまま提供する。
- 内部のフラグ、段階名、因果要約、制作工程名を画面へ直書きしない。人物の反応、場所の
  変化、実況、劇中の道具、明確な操作名へ翻訳する。
- 同じ意味を、章名、眉見出し、カード見出し、説明文、CTAで繰り返さない。一度で伝える。
- 英語は作品固有名、自然な劇中看板、一般的な単位など、世界または操作に意味がある場合
  だけ使う。商業感を出すための飾り英語は禁止する。

## 物語と選択結果

- 選択後に独立した報告書型の結果画面を挟まない。選択の直後も通常の物語として続け、
  人物と世界が反応する場面を一枚絵、背景、立ち絵、会話、地の文で見せる。
- 「あなたが選んだ」「今週の結果」「この選択が残したもの」「選択で変わったこと」など、
  制作者が因果を整理する可視ラベルを使わない。直前の選択を再掲して説明しない。
- 数値変化は物語の余韻の後、必要なものだけを小さな非モーダル通知で示す。数値カードで
  一枚絵や会話を覆わない。
- 一枚絵は一会話で消さず、一つの場面として文章が終わるまで保持する。記録室では同じ
  場面の文章と合わせて追体験できるようにする。
- 背景、人物、表情、一枚絵、文章の場所・時刻・行為が一致しない状態をバグとして扱う。

## 世界と題材

- 物語の核は、この作品でしか起きない世界の発見、裏ボス級人物との乙女ゲーム文法の
  出会い、明るく笑えて誰かへ勧めたくなる驚きに置く。
- 職場のいじめ、退職予想、陰口、同僚からの承認・歓迎、社内評価を、安易な葛藤や感動の
  主題にしない。現実の職場題材を使う場合も、世界を見せるために本当に必要か、対象人物を
  傷つけて笑いにしていないかを事前に検査する。
- 初見プレイヤーだけが知らない固有語や省略参照を単体で出さない。新しい仕組みや用語は、
  同じ場面で「何であり、何が起きるか」が分かるように描く。
- 各話は実装前に、世界の新情報、楽しい感情、次を見たい予兆、画との一致、安易な職場
  人間関係への逃避がないことを確認する。

## 画面検証

- 週契約画面も既得の完成形として扱わず、全画面を同じマニフェストで監査する。
- 800x450の内部画面と844x390のスマートフォン横画面を実ブラウザで確認する。
- 文字、人物の顔・手・重心、主要CTA、下端を互いに遮らない。縮小後の本文を実寸10px未満、
  補助文字を実寸9px未満にしない。
- ビルドやDOM寸法だけで完成としない。スクリーンショットを目視し、最初から次画面まで
  実際にクリックして通ることを確認する。
- 既存の未コミット変更を巻き戻さない。担当外のバトル専用CSSや物語本文へ変更が必要な
  場合は、担当タスクへ具体的な再現条件と完成条件を渡す。

## 継続作業の軽量化

- 作業を途中で打ち切るためではなく、同じ目的へ継続して進むために一回あたりの負荷を抑える。
- 検索・読み取りは `mimi_secret_boss_arena` 直下だけを既定範囲とし、親の `projects` 全体を
  探すのは所在不明の依存物がある場合に限る。
- 小さな修正では補助エージェントを増やさない。独立して並行でき、待ち時間を実際に短縮する
  作業だけを委譲し、完了した補助タスクはすぐ閉じる。
- ファイル読み取りは常駐の軽量経路を優先し、PowerShell起動はテスト、ビルド、Git確認など
  シェルが必要な処理へ限定する。
- 編集は関連箇所を一度に読み、一つのパッチへまとめる。編集のたびに全検索、全ビルド、全画面
  QAを繰り返さない。
- 実ブラウザは既存タブと既存サーバーを再利用し、対象画面の確認を短い反復で行う。総合テストと
  全画面通しは関連変更がまとまった節目で一度実行する。
- 長いコマンド出力をそのまま取得しない。対象ファイル、行、件数、エラーへ絞り、出力上限を
  小さく保つ。
